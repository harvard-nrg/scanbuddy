[package]
name = "scanbuddy_snr"
version = "0.1.0"
edition = "2021"

[lib]
name = "scanbuddy_snr"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
numpy = "0.24"
ndarray = "0.16"
